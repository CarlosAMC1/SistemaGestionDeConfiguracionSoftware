@model IEnumerable<SistemaGestionDeConfiguracionSoftware.Models.TAREA>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NOMBRE)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.JUSTIFICACION)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CODIGO)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NOMBRE)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.JUSTIFICACION)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CODIGO)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_TAREA }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_TAREA }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_TAREA })
        </td>
    </tr>
}

</table>
